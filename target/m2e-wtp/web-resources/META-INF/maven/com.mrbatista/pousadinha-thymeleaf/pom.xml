<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
 
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.mrbatista</groupId>
  <artifactId>pousadinha-thymeleaf</artifactId>
  <version>1.0.0-SNAPSHOT</version>
 
  <packaging>war</packaging>

  <properties>
		<project.buid.sourceEncoding>UTF-8</project.buid.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<failOnMissingWebXml>false</failOnMissingWebXml>

		<java.version>21</java.version>
		<maven-compiler-pluging.version>3.14.1</maven-compiler-pluging.version>		
	</properties>
	
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-pluging.version}</version> <!--
				Certifique-se de usar a versão mais recente -->
				<configuration>
					<source>${java.version}</source> <!-- Versão do Java usada no
					código-fonte -->
					<target>${java.version}</target> <!-- Versão do Java para o
					bytecode gerado -->
				</configuration>
			</plugin>
		</plugins>
	</build>  
</project>